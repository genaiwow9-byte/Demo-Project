apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: hello-word-sm
  namespace: monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: hello-word
  namespaceSelector:
    matchNames:
    - default
  endpoints:
  - port: http
    path: /metrics
    interval: 15s
